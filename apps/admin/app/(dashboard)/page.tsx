import { DashboardPage } from '@/features/analytics/pages/DashboardPage';

/** AdminDashboard — UI-API deep link `/` (P2-ADM-02). */
export default function AdminDashboardRoute() {
  return <DashboardPage />;
}

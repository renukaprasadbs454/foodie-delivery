import { AnalyticsPage } from '@/features/analytics/pages/AnalyticsPage';

/** AdminAnalytics — UI-API deep link `/analytics` (P2-ADM-02). */
export default function AdminAnalyticsRoute() {
  return <AnalyticsPage />;
}

import { FoundationPlaceholder } from '@/components/DashboardShell';

/** SUPER_ADMIN route shell — System Design §5.3. No audit table UI. */
export default function AuditLogPage() {
  return <FoundationPlaceholder title="Audit Log" />;
}
